# ✍️ Personal Blog Application Backend

A modern, secure, and scalable backend for a personal blogging platform, built using **Spring Boot 3**. This application is designed to power my personal blog, enabling me to **share notes and experiences**, and serves as the API foundation for syncing content with  [Μy Medium Account](https://medium.com/@didimasmail)
.

It features robust **Spring Security** with **JWT** for secure user management and uses **MySQL** as the primary data store.

---

## 🚀 Key Features

### 📝 Core Functionality
* **Post Management** (CRUD operations for creating, reading, updating, and deleting blog posts)
* **Tagging System** for post categorization
* **User Authentication** (Registration/Login)
* **Secure Endpoints** protected with JWT
* **Post Filtering** by tags or title keywords
* **Pagination & Sorting**

---

### 🛡 Security
* **Spring Security** Integration
* **JWT** Authentication for stateless operations
* **Role-based** authorization (Admin/User)
* **Password Hashing** (**BCrypt**)

---

### ⚙ Architecture / Engineering
* **Spring Data JPA** (Hibernate)
* **MySQL** for relational data persistence
* **Lombok** for boilerplate reduction
* **Maven** build tool
* **Request Validation** (`jakarta.validation`)
* **Swagger OpenAPI** documentation
* **Clean DTO-based architecture**

---

## 🧰 Tech Stack Summary

| Layer | Technology | Key Use |
| :--- | :--- | :--- |
| **Backend** | **Spring Boot 3, Spring Web** | Framework for REST APIs |
| **Database** | **MySQL** | Primary relational data store |
| **Security** | **Spring Security, JWT** | Authentication & Authorization |
| **ORM** | **Hibernate + Spring Data JPA** | Database interaction |
| **Build Tool** | **Maven** | Dependency management |
| **Documentation**| **Swagger / OpenAPI** | Interactive API documentation |

---

## 📁 Project Structure

The project structure follows best practices for a Spring Boot application:

```text
blog-app/
 ├── controller/        # API Endpoints (Handling HTTP requests)
 ├── service/           # Business Logic (Core application rules)
 ├── model/             # Database Models (JPA Entities)
 ├── repository/        # Data Access Layer (JPA Repositories)
 ├── dto/               # Data Transfer Objects (Request/Response)
 ├── security/          # JWT/Spring Security Configuration
 ├── config/            # Application configuration
 ├── exception/         # Custom exception handling
 ├── resources/
 │   └── application.yml # Configuration files
 └── pom.xml            # Maven project file
```

## ⚙️ Installation & Running the Backend

### ✅ Prerequisites


Ensure the following are installed:

-   **Java 17+**
    
-   **Maven 3+**
    
-   **MySQL 8+** 
    
-   **Git**
    

### 🏗 Step 1: Clone the Repository


Bash

    git clone [https://github.com/DimitrisDimas/blog-app.git](https://github.com/DimitrisDimas/blog-app.git)
    cd blog-app

### 🗄 Step 2: Create MySQL Database


You will need a MySQL database instance.

```SQL
    CREATE DATABASE blog;
```

 **Note:** Update database connection details in `src/main/resources/application.properties`:

``` application.properties
    spring:
      datasource:
       url: jdbc:mysql://localhost:3307/blog
       username: your_username
       password: your_password
````
### 📦 Step 3: Build and Run



```Bash
    mvn clean install
```
```
    mvn spring-boot:run
```
Your backend will be running on: **`http://localhost:8080`**


## 📘 API Documentation (Swagger)


The interactive API documentation is automatically generated by Swagger/OpenAPI and is available at:

🔗 **`http://localhost:8080/swagger-ui/index.html`**

* * *

## 🔑 Authentication

### Register


`POST /api/auth/register`

### Login


`POST /api/auth/login`

**Login Usage:** The response will contain a JWT token. Use this token in the `Authorization` header for protected endpoints:

`Authorization: Bearer <your.jwt.token>`


# 📚 REST API Endpoints Overview


Below is a summarized list of all the core REST APIs provided by the application.
All protected routes require a valid JWT Bearer Token.


## 🔐 **Auth APIs**


| Method | Endpoint | Description | Auth |
| --- | --- | --- | --- |
| POST | `/api/auth/register` | Register new user | ❌ |
| POST | `/api/auth/login` | Login and receive JWT token | ❌ |


## 🏷 **Category APIs**


| Method | Endpoint | Description | Auth |
| --- | --- | --- | --- |
| POST | `/api/categories` | Create new category | ✔ Admin |
| GET | `/api/categories` | Get all categories (paginated) | ❌ |
| GET | `/api/categories/{id}` | Get category by ID | ❌ |
| PUT | `/api/categories/{id}` | Update category | ✔ Admin |
| DELETE | `/api/categories/{id}` | Delete category | ✔ Admin |


## 📝 **Post APIs**


| Method | Endpoint | Description | Auth |
| --- | --- | --- | --- |
| POST | `/api/posts` | Create new post | ✔ Admin |
| GET | `/api/posts` | Get all posts (pagination, sorting, filtering) | ❌ |
| GET | `/api/posts/{id}` | Get post by ID | ❌ |
| PUT | `/api/posts/{id}` | Update post | ✔ Admin |
| DELETE | `/api/posts/{id}` | Delete post | ✔ Admin |
| GET | `/api/posts/category/{id}` | Get posts by category | ❌ |


## 💬 **Comment APIs**

| Method | Endpoint | Description | Auth |
| --- | --- | --- | --- |
| POST | `/api/posts/{postId}/comments` | Add comment to post | ❌ |
| GET | `/api/posts/{postId}/comments` | Get all comments for post | ❌ |
| GET | `/api/posts/{postId}/comments/{id}` | Get comment by ID | ❌ |
| PUT | `/api/posts/{postId}/comments/{id}` | Update comment | ✔ User/Admin |
| DELETE | `/api/posts/{postId}/comments/{id}` | Delete comment | ✔ User/Admin |


## 🧪 Testing & Code Quality

The application implements both system-level and unit-level testing to ensure stability, correctness, and long-term maintainability.

## 🧩 Unit Tests

Written using JUnit 5 and Mockito, covering:

* Service layer (business logic)

* Repository interactions (mocked)

* Validation rules

* JWT utility components

## 🧪 Integration / Web Layer Tests

Using:

* @WebMvcTest for controller-level testing

* MockMvc for HTTP endpoint simulation

* JSON request/response validation

* Spring Security test utilities for authentication and authorization scenarios

### 💡 **Goal:** Gradually increase test coverage to **80%+**, ensuring the project remains stable, reliable, and production-ready.

## 📌 Roadmap & Future Plans

### 

This is the foundational backend for the personal blog. Future developments include:

-   **Frontend Development (In Progress):** Integrating a modern JavaScript frontend (e.g., React/Vue) to consume these APIs.
    
-   **External Integration:** Implementing webhooks or scheduled jobs to synchronize posts with my **Medium publication**.
    
-   **Admin Dashboard:** Creating dedicated endpoints for administrative tasks.
    
-   **CORS Configuration:** Fine-tuning security headers for production deployment.
